import 'package:flutter/material.dart';

import 'package:resep_makanan_sederhana_dr_dicoding_kelas_flutter/main_page.dart';

void main() => runApp(Makanan2App());

class Makanan2App extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: MainScreen(),
    );
  }
}